# ═══════════════════════════════════════
# CARAVAN / ZeroG — Web Configuration
# ═══════════════════════════════════════

# ── API ───────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:5180

# ── Site ──────────────────────────────
# Canonical site URL (used for metadata, robots.txt, sitemap)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── OAuth ─────────────────────────────
# Google OAuth client ID (must match server-side GOOGLE_CLIENT_ID)
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ── Push Notifications (VAPID) ───────
# Public VAPID key for Web Push subscriptions
# Generate a VAPID key pair: npx web-push generate-vapid-keys
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=

# ── AI Assistant ──────────────────────
# Google Gemini API key for AI features
# NEXT_PUBLIC_GEMINI_API_KEY=

# ── Analytics (optional) ─────────────
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
