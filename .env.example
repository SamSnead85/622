# ═══════════════════════════════════════
# 0G — Root .env
# ═══════════════════════════════════════
#
# This monorepo uses per-app environment files.
# See the app-specific examples for full configuration:
#
#   apps/server/.env.example   — API server (Express, Prisma, Redis)
#   apps/web/.env.example      — Web client (Next.js)
#
# Copy the relevant example into each app directory:
#
#   cp apps/server/.env.example apps/server/.env
#   cp apps/web/.env.example    apps/web/.env
#
